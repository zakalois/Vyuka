@page
@model Vyuka.Pages.Teachers_only.TeacherScheduleModel

@{
    Layout = "_TeacherLayout";
}

<div class="container mt-4">

    <!-- Navigace týdnů -->
    <div class="d-flex justify-content-between align-items-center mb-3 flex-wrap">
        <a class="btn btn-secondary" asp-page="./TeacherSchedule" asp-route-week="@Model.PreviousWeek">
            ← Předchozí týden
        </a>

        <div class="text-center mx-2">
            <h5 class="mb-1">@Model.StartOfWeek.ToString("dd.MM.yyyy") – @Model.EndOfWeek.ToString("dd.MM.yyyy")</h5>
            <a class="btn btn-info btn-sm" asp-page="./TeacherSchedule" asp-route-week="0">
                Aktuální týden
            </a>
        </div>

        <a class="btn btn-secondary" asp-page="./TeacherSchedule" asp-route-week="@Model.NextWeek">
            Další týden →
        </a>
    </div>


    <!-- 🟩 EDITACE -->
    @if (Model.EditingLesson != null)
    {
        <div class="card shadow-sm mb-4">
            <div class="card-header bg-primary text-white text-center">
                <strong>Upravit hodinu / plán</strong>
            </div>

            <div class="card-body">

                <form method="post" asp-page-handler="EditSave">

                    <input type="hidden" name="id" value="@(Model.EditingLesson.LessonPlanId ?? Model.EditingLesson.LessonId)" />
                    <input type="hidden" name="isPlan" value="true" />

                    <div class="row g-2">

                        <div class="col-md-3">
                            <label class="form-label">Datum</label>
                            <input name="date" type="date"
                                   value="@Model.EditingLesson.Date.ToString("yyyy-MM-dd")"
                                   class="form-control" />
                        </div>

                        <div class="col-md-2">
                            <label class="form-label">Začátek</label>
                            <input name="start" type="time"
                                   value="@Model.EditingLesson.Start.ToString(@"hh\:mm")"
                                   class="form-control" />
                        </div>

                        <div class="col-md-2">
                            <label class="form-label">Konec</label>
                            <input name="end" type="time"
                                   value="@Model.EditingLesson.End.ToString(@"hh\:mm")"
                                   class="form-control" />
                        </div>

                        <div class="col-md-3">
                            <label class="form-label">Předmět</label>
                            <select name="subjectId" class="form-select">
                                @foreach (var s in Model.Subjects)
                                {
                                    <option value="@s.Id" selected="@(s.Id == Model.EditingLesson.SubjectId)">
                                        @s.Name
                                    </option>
                                }
                            </select>
                        </div>

                        <div class="col-md-4 mt-2">
                            <label class="form-label">Student</label>
                            <select name="studentId" class="form-select">
                                @foreach (var s in Model.Students)
                                {
                                    <option value="@s.Id" selected="@(s.Id == Model.EditingLesson.StudentId)">
                                        @s.LastName @s.FirstName
                                    </option>
                                }
                            </select>
                        </div>

                        <div class="col-md-12 mt-2">
                            <label class="form-label">Meet link</label>
                            <input name="meetLink" class="form-control" value="@Model.EditingLesson.MeetLink" />
                        </div>

                    </div>

                    <button type="submit" class="btn btn-success w-100 mt-3">Uložit změny</button>

                </form>

            </div>
        </div>
    }



    <!-- 🟨 PŘIDAT HODINU -->
    @if (Model.EditingLesson == null)
    {
        <div class="card shadow-sm mb-4">
            <div class="card-header bg-success text-white text-center">
                <strong>Přidat hodinu</strong>
            </div>

            <div class="card-body">
                <form method="post" asp-page="/Teachers_only/TeacherSchedule" asp-page-handler="AddLesson">

                    <input type="hidden" name="week" value="@Model.Week" />

                    <div class="row">

                        <div class="col-md-3">
                            <label class="form-label">Student</label>
                            <select asp-for="NewLesson.StudentId" class="form-select">
                                @foreach (var s in Model.Students)
                                {
                                    <option value="@s.Id">@s.LastName @s.FirstName</option>
                                }
                            </select>
                        </div>

                        <div class="col-md-3">
                            <label class="form-label">Předmět</label>
                            <select asp-for="NewLesson.SubjectId" class="form-select">
                                @foreach (var s in Model.Subjects)
                                {
                                    <option value="@s.Id">@s.Name</option>
                                }
                            </select>
                        </div>

                        <div class="col-md-2">
                            <label class="form-label">Datum</label>
                            <input asp-for="NewLesson.Date" type="date" class="form-control" />
                        </div>

                        <div class="col-md-2">
                            <label class="form-label">Začátek</label>
                            <input asp-for="NewLesson.Start" type="time" class="form-control" />
                        </div>

                        <div class="col-md-2">
                            <label class="form-label">Konec</label>
                            <input asp-for="NewLesson.End" type="time" class="form-control" />
                        </div>

                        <div class="col-md-12 mt-2">
                            <label class="form-label">Meet link</label>
                            <input asp-for="NewLesson.MeetLink" class="form-control" />
                        </div>

                    </div>

                    <button type="submit" class="btn btn-success w-100 mt-3">
                        Přidat hodinu
                    </button>

                </form>



            </div>
        </div>
    }


    <!-- 🟦 TÝDENNÍ ROZVRH -->
    <div class="card shadow-sm mb-4">
        <div class="card-header bg-warning text-center">
            <strong>Týdenní rozvrh</strong>
        </div>

        <div class="card-body">
            <div class="schedule-grid">

                @foreach (var day in Model.WeeklySchedule)
                {
                    string dayClass = day.Day switch
                    {
                        "Sobota" => "day-sat",
                        "Neděle" => "day-sun",
                        _ => ""
                    };

                    <div class="day-column @dayClass">
                        <div class="day-header">
                            <strong>@day.Day</strong><br />
                            <small class="text-muted">@day.Date.ToString("dd.MM.")</small><br />
                            <span class="text-muted">@day.Lessons.Sum(l => (l.End - l.Start).TotalHours) hodin</span>
                        </div>

                        <hr />

                        @if (day.Lessons.Any())
                        {
                            foreach (var l in day.Lessons)
                            {
                                <div class="lesson-block @(l.IsTaught ? "taught" : "")" style="background-color:@l.Color">

                                    <div class="lesson-body">
                                        <strong>@l.Student</strong><br />
                                        <span class="text-muted">@l.Subject</span><br />
                                        @l.Start.ToString(@"hh\:mm") – @l.End.ToString(@"hh\:mm")<br />

                                        @if (!string.IsNullOrEmpty(l.MeetLink))
                                        {
                                            <a href="@l.MeetLink" target="_blank" class="meet-link">Meet</a>
                                        }

                                        <div class="lesson-icons mt-1">

                                            @if (l.LessonPlanId != null)
                                            {
                                                <form method="get" class="d-inline">
                                                    <input type="hidden" name="editId" value="@l.LessonPlanId" />
                                                    <input type="hidden" name="isPlan" value="true" />
                                                    <input type="hidden" name="week" value="@Model.Week" />
                                                    <button class="icon-btn">✏️</button>
                                                </form>
                                            }

                                            @if (l.LessonId != null)
                                            {
                                                <form method="post" asp-page-handler="DeleteLesson" class="d-inline">
                                                    <input type="hidden" name="id" value="@l.LessonId" />
                                                    <input type="hidden" name="week" value="@Model.Week" />
                                                    <button class="icon-btn">🗑️</button>
                                                </form>
                                            }

                                            @if (l.LessonPlanId != null)
                                            {
                                                <form method="post" asp-page-handler="DeletePlan" class="d-inline">
                                                    <input type="hidden" name="id" value="@l.LessonPlanId" />
                                                    <input type="hidden" name="week" value="@Model.Week" />
                                                    <button class="icon-btn">🗑️</button>
                                                </form>
                                            }

                                            @if (!l.IsTaught)
                                            {
                                                <form method="post" asp-page-handler="TeachLesson" class="d-inline">
                                                    <input type="hidden" name="id" value="@(l.LessonId ?? l.LessonPlanId)" />
                                                    <input type="hidden" name="week" value="@Model.Week" />
                                                    <button class="icon-btn">✔️</button>
                                                </form>
                                            }

                                        </div>

                                    </div>
                                </div>
                            }
                        }
                        else
                        {
                            <div class="text-muted small">Žádné hodiny</div>
                        }
                    </div>
                }
            </div>
        </div>
    </div>
</div>
